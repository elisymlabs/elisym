{
  "name": "@elisym/config-client",
  "version": "0.1.0",
  "private": true,
  "files": [
    "dist",
    "src"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "node ./codama.config.mjs",
    "build": "bun run generate && tsup src/index.ts --format esm --dts --sourcemap --clean",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests'",
    "clean": "rm -rf dist src/generated"
  },
  "dependencies": {
    "@solana/kit": "~6.8.0"
  },
  "devDependencies": {
    "@codama/nodes-from-anchor": "~1.3.0",
    "@codama/renderers-js": "~1.3.0",
    "codama": "~1.4.0",
    "tsup": "~8.4.0",
    "typescript": "~5.7.0"
  }
}
